{
    "name": "web-continuous-testing-framework-v1",
    "version": "1.0.0",
    "description": "Framework de automatización de pruebas WEB",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*",
        "templates/",
        "scripts/"
    ],
    "scripts": {
        "build": "tsc --project tsconfig.json",
        "build:watch": "tsc --project tsconfig.json --watch",
        "prepublishOnly": "npm run build",
        "postinstall": "npm run build && node scripts/ensure-browser.js",
        "ensure-browser": "node scripts/ensure-browser.js",
        "test": "npx playwright test"
    },
    "keywords": [],
    "author": "",
    "license": "",
    "devDependencies": {
        "@cucumber/cucumber": "^12.2.0",
        "@types/node": "^24.1.0",
        "typescript": "^5.8.3",
        "pino-pretty": "^13.1.1"
    },
    "dependencies": {
        "@playwright/test": "^1.54.2",
        "csv-parser": "^3.2.0",
        "docx-templates": "^4.14.1",
        "pino": "^9.10.0"
    }
}